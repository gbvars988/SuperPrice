import Button from '@mui/material/Button';
/*
interface CartIconInterface {
  large?: boolean;
}

export const SCartIcon = <CartIconInterface>`
  width: ${({ large })} => (large ? '60px' : '50px')};
  height: ${({ large })} => (large ? '60px' : '50px')};
  position: relative;
  display: inline-block;
  vertical-align: middle;
  margin-right: 15px;
  background-image: url(${require('cart.png')});
  background-repeat: no-repeat;
  background-size: contain;
  background-position: center;
  background-size: 50%;
`;
*/
/*
export const CartQuantity = div`
  display: inline-block;
  width: 18px;
  height: 18px;
  color: #0c0b10;
  font-weight: bold;
  font-size: 0.7em;
  text-align: center;
  line-height: 18px;
  border-radius: 50%;
  background-color: ${({ theme }) => theme.colors.secondary};
  position: absolute;
  bottom: 0;
  right: 5px;
`;
*/